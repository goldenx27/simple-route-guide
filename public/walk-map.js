(()=>{
  const WALK_SEGMENTS={
    'maor-home-school':['home-to-38283','38252-to-school'],
    'maor-school-home':['school-to-36743','33734-to-home']
  };
  const ROUTE_NEAR_METERS=90, START_NEAR_METERS=100, DEST_NEAR_METERS=75, LANDMARK_SHOW_MS=6500;
  let map=null,userMarker=null,routeHalo=null,routeLine=null,passedLine=null,signalLayer=null,mapWatch=null,lastFix=null,lastFixAt=0,displayFix=null,leafletLoading=null,signalsKey='',lastLandmarkKey='',landmarkTimer=null,landmarkVisible=false,lastRouteIndex=-1,routeHeading=0,lastMapRouteKey='';

  function addStyles(){if(document.getElementById('walkMapStyles'))return;const s=document.createElement('style');s.id='walkMapStyles';s.textContent=`
    #walkMiniMap{height:230px;border-radius:18px;overflow:hidden;margin:0;direction:ltr;background:#edf1f4;box-shadow:inset 0 0 0 1px #dfe5ea;position:relative;flex:0 0 230px;order:1}
    #walkMiniMap.hidden{display:none!important}.leaflet-control-attribution{font-size:8px!important;opacity:.55}.leaflet-control-zoom{display:none}
    #walkMiniMap .leaflet-map-pane,#walkMiniMap .leaflet-tile-pane,#walkMiniMap .leaflet-overlay-pane{rotate:0deg!important;transform-origin:center center!important}
    .maor-arrow{width:30px;height:30px;border-radius:50%;background:#1677ff;border:4px solid white;box-shadow:0 2px 8px rgba(0,0,0,.35);display:flex;align-items:center;justify-content:center;color:#fff;font-size:15px;line-height:1;transform:rotate(var(--heading,0deg))}
    .route-signal{font-size:22px;filter:drop-shadow(0 1px 2px white)}
    html body #trip .content{position:relative!important;overflow:hidden!important;justify-content:flex-end!important;gap:10px!important}
    html body #trip .content #landmarkFeedback{position:relative!important;left:auto!important;top:auto!important;transform:none!important;width:100%!important;max-width:100%!important;height:230px!important;min-height:230px!important;max-height:230px!important;flex:0 0 230px!important;order:1!important;padding:8px!important;margin:0!important;background:#fff!important;border:2px solid #b9e7c7!important;border-radius:18px!important;box-shadow:0 5px 18px rgba(0,0,0,.10)!important;overflow:hidden!important;display:flex!important;flex-direction:column!important;justify-content:flex-start!important;box-sizing:border-box!important}
    html body #trip .content #landmarkFeedback.hidden{display:none!important}
    #landmarkFeedback .landmark-all-photos{height:165px;display:grid;gap:5px;grid-template-columns:1fr;grid-template-rows:1fr;direction:ltr;overflow:hidden;border-radius:13px;flex:0 0 165px}
    #landmarkFeedback .landmark-all-photos.count-2{grid-template-columns:1fr 1fr}
    #landmarkFeedback .landmark-all-photos.count-3{grid-template-columns:1fr 1fr;grid-template-rows:1fr 1fr}
    #landmarkFeedback .landmark-all-photos.count-3 img:first-child{grid-row:1 / span 2}
    #landmarkFeedback .landmark-all-photos.count-4{grid-template-columns:1fr 1fr;grid-template-rows:1fr 1fr}
    #landmarkFeedback .landmark-all-photos.count-many{grid-template-columns:repeat(3,1fr);grid-auto-rows:1fr}
    html body #trip .content #landmarkFeedback .landmark-all-photos img{display:block!important;width:100%!important;height:100%!important;min-height:0!important;max-height:none!important;object-fit:cover!important;border-radius:8px!important;flex:none!important}
    html body #trip .content #landmarkFeedback .ok{font-size:.86rem!important;line-height:1.15!important;margin-top:6px!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
    html body #trip .content #landmarkFeedback .where{font-size:.72rem!important;line-height:1.15!important;margin-top:3px!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
    html body #trip .content #landmarkFeedback .landmark-dots{display:none!important}
    html body #trip .content>.card{order:3!important;flex:0 0 auto!important}
    #routeStartNotice{position:absolute;z-index:50;left:10px;right:10px;bottom:10px;background:#fff;border-radius:16px;padding:12px;box-shadow:0 8px 24px rgba(0,0,0,.18);text-align:center;font-weight:800;direction:rtl}
    @media(max-height:760px){#walkMiniMap{height:195px;flex-basis:195px}html body #trip .content #landmarkFeedback{height:195px!important;min-height:195px!important;max-height:195px!important;flex-basis:195px!important}#landmarkFeedback .landmark-all-photos{height:132px;flex-basis:132px}}
  `;document.head.appendChild(s)}
  function loadLeaflet(){if(window.L)return Promise.resolve();if(leafletLoading)return leafletLoading;leafletLoading=new Promise((resolve,reject)=>{const css=document.createElement('link');css.rel='stylesheet';css.href='https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';document.head.appendChild(css);const js=document.createElement('script');js.src='https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';js.onload=resolve;js.onerror=reject;document.head.appendChild(js)});return leafletLoading}
  function point(p){return p&&Number.isFinite(Number(p.lat))&&Number.isFinite(Number(p.lon))?{lat:Number(p.lat),lon:Number(p.lon)}:null}
  function pointsForSegment(k){return(routeRecordings[k]?.points||[]).map(point).filter(Boolean)}
  function pointsForRoute(){const segs=WALK_SEGMENTS[selectedRoute]||[];return segs.flatMap(pointsForSegment)}
  function destinationPoints(){const segs=WALK_SEGMENTS[selectedRoute]||[];return pointsForSegment(segs[1])}
  function activeSegmentKey(){if(current?.step?.type!=='walk')return'';const segs=WALK_SEGMENTS[selectedRoute]||[];return current?.trip?.current_step<=1?segs[0]||'':segs[1]||''}
  function activeWalkPoints(){return pointsForSegment(activeSegmentKey())}
  function nearest(here,pts){let best={distance:Infinity,index:-1};pts.forEach((p,i)=>{const d=distanceMeters(here,p);if(d<best.distance)best={distance:d,index:i}});return best}
  function nearestProgressive(here,pts){const start=Math.max(0,lastRouteIndex-6),end=Math.min(pts.length-1,lastRouteIndex<0?pts.length-1:lastRouteIndex+40);let best={distance:Infinity,index:-1};for(let i=start;i<=end;i++){const d=distanceMeters(here,pts[i]);if(d<best.distance)best={distance:d,index:i}}return best.index>=0?best:nearest(here,pts)}
  function bearing(a,b){const p1=a.lat*Math.PI/180,p2=b.lat*Math.PI/180,dl=(b.lon-a.lon)*Math.PI/180;return(Math.atan2(Math.sin(dl)*Math.cos(p2),Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dl))*180/Math.PI+360)%360}
  function smoothPoint(prev,next,alpha=.28){if(!prev)return next;return{lat:prev.lat+(next.lat-prev.lat)*alpha,lon:prev.lon+(next.lon-prev.lon)*alpha}}
  function ensureContainer(){const content=document.querySelector('#trip .content');if(!content)return null;let box=document.getElementById('walkMiniMap');if(!box){box=document.createElement('div');box.id='walkMiniMap';box.className='hidden';content.insertBefore(box,content.firstChild)}return box}
  async function ensureMap(){const box=ensureContainer();if(!box)return;await loadLeaflet();if(map)return;map=L.map(box,{zoomControl:false,attributionControl:true,dragging:false,doubleClickZoom:false,scrollWheelZoom:false,touchZoom:false,boxZoom:false,keyboard:false,zoomAnimation:false,fadeAnimation:false,markerZoomAnimation:false});L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap',updateWhenIdle:true,keepBuffer:4}).addTo(map);signalLayer=L.layerGroup().addTo(map)}
  function markerIcon(){return L.divIcon({className:'',html:`<div class="maor-arrow" style="--heading:${routeHeading}deg">▲</div>`,iconSize:[30,30],iconAnchor:[15,15]})}
  function signalIcon(){return L.divIcon({className:'',html:'<div class="route-signal">🚦</div>',iconSize:[26,26],iconAnchor:[13,13]})}
  async function fetchSignals(pts){if(!pts.length||!signalLayer)return;const key=`${pts[0].lat.toFixed(3)}:${pts[0].lon.toFixed(3)}:${pts.at(-1).lat.toFixed(3)}:${pts.at(-1).lon.toFixed(3)}`;if(key===signalsKey)return;signalsKey=key;const lats=pts.map(p=>p.lat),lons=pts.map(p=>p.lon),pad=.002;const bbox=`${Math.min(...lats)-pad},${Math.min(...lons)-pad},${Math.max(...lats)+pad},${Math.max(...lons)+pad}`;try{const q=`[out:json][timeout:8];node[highway=traffic_signals](${bbox});out;`;const r=await fetch('https://overpass-api.de/api/interpreter',{method:'POST',body:q});const d=await r.json();signalLayer.clearLayers();(d.elements||[]).forEach(x=>{const p={lat:x.lat,lon:x.lon};if(nearest(p,pts).distance<=45)L.marker([p.lat,p.lon],{icon:signalIcon(),interactive:false}).addTo(signalLayer)})}catch(e){}}
  function resetTrackingForSegment(){lastFix=null;lastFixAt=0;displayFix=null;lastRouteIndex=-1;routeHeading=0;lastLandmarkKey='';if(userMarker){userMarker.remove();userMarker=null}if(passedLine){passedLine.remove();passedLine=null}}
  function drawRouteOnce(pts,key,here){if(key===lastMapRouteKey&&routeLine)return;lastMapRouteKey=key;resetTrackingForSegment();for(const l of [routeHalo,routeLine,passedLine])if(l)l.remove();routeHalo=routeLine=passedLine=null;const coords=pts.map(p=>[p.lat,p.lon]);routeHalo=L.polyline(coords,{weight:13,opacity:.92,color:'#fff',lineCap:'round',lineJoin:'round',interactive:false}).addTo(map);routeLine=L.polyline(coords,{weight:7,opacity:.95,color:'#1677ff',lineCap:'round',lineJoin:'round',interactive:false}).addTo(map);const bounds=routeLine.getBounds();if(here)bounds.extend([here.lat,here.lon]);map.fitBounds(bounds,{padding:[28,28],maxZoom:18,animate:false});setTimeout(()=>map?.invalidateSize(false),40);fetchSignals(pts)}
  function updatePassedLine(pts){if(passedLine)passedLine.remove();passedLine=null;if(lastRouteIndex>0){passedLine=L.polyline(pts.slice(0,lastRouteIndex+1).map(p=>[p.lat,p.lon]),{weight:7,opacity:.95,color:'#6b7280',lineCap:'round',lineJoin:'round',interactive:false}).addTo(map)}}
  function hideLandmarkAndRestoreMap(){landmarkVisible=false;clearTimeout(landmarkTimer);landmarkTimer=null;document.getElementById('landmarkFeedback')?.classList.add('hidden');const box=document.getElementById('walkMiniMap');if(box&&current?.step?.type==='walk'){box.classList.remove('hidden');setTimeout(()=>map?.invalidateSize(false),30)}}
  function showLandmarkTemporarily(lm){const photos=Array.isArray(lm?.photos)?lm.photos.filter(Boolean):[];if(!photos.length)return;landmarkVisible=true;if(typeof landmarkSlideTimer!=='undefined'&&landmarkSlideTimer){clearInterval(landmarkSlideTimer);landmarkSlideTimer=null}const box=document.getElementById('landmarkFeedback');if(!box)return;const countClass=photos.length===1?'count-1':photos.length===2?'count-2':photos.length===3?'count-3':photos.length===4?'count-4':'count-many';box.innerHTML=`<div class="landmark-all-photos ${countClass}">${photos.map((src,i)=>`<img src="${src}" alt="${(lm.name||'נקודת ציון').replace(/"/g,'&quot;')} ${i+1}"/>`).join('')}</div><div class="ok">✅ אתה בדרך הנכונה</div><div class="where">${lm.name||'המקום שסימנתם'}</div>`;box.classList.remove('hidden');document.getElementById('walkMiniMap')?.classList.add('hidden');clearTimeout(landmarkTimer);landmarkTimer=setTimeout(hideLandmarkAndRestoreMap,LANDMARK_SHOW_MS)}
  function showNearbyLandmark(here,accuracy){const seg=typeof segmentForCurrentStep==='function'?segmentForCurrentStep():null,rec=seg&&routeRecordings[seg];if(!rec)return;let best=null,dist=Infinity;for(const lm of rec.landmarks||[]){if(!lm.photos?.length)continue;const d=distanceMeters(here,lm);if(d<dist){best=lm;dist=d}}const threshold=Math.max(24,Math.min(50,(accuracy||12)+18));if(best&&dist<=threshold){const key=`${seg}:${best.name}:${best.lat}:${best.lon}`;if(key!==lastLandmarkKey){lastLandmarkKey=key;showLandmarkTemporarily({...best,distance_m:Math.round(dist)})}}else if(dist>threshold+25)lastLandmarkKey=''}
  async function paintMap(here,accuracy){if(current?.step?.type!=='walk'||landmarkVisible)return;const pts=activeWalkPoints();await ensureMap();const box=document.getElementById('walkMiniMap');box.classList.remove('hidden');if(pts.length<2){routeHeading=Number.isFinite(Number(lastFix?.heading))?Number(lastFix.heading):0;displayFix=smoothPoint(displayFix,here,displayFix?.lat?0.35:1);if(!userMarker)userMarker=L.marker([displayFix.lat,displayFix.lon],{icon:markerIcon(),interactive:false,zIndexOffset:1000}).addTo(map);else userMarker.setLatLng([displayFix.lat,displayFix.lon]);map.setView([displayFix.lat,displayFix.lon],18,{animate:false});return}
    const routeKey=`${selectedRoute}:${activeSegmentKey()}:${pts.length}`;drawRouteOnce(pts,routeKey,here);const near=nearestProgressive(here,pts);const snapLimit=Math.max(45,Math.min(95,(accuracy||15)+35));let target=here;
    if(near.index>=0&&near.distance<=snapLimit){let candidate=near.index;if(lastRouteIndex>=0){if(candidate<lastRouteIndex-4)candidate=lastRouteIndex;if(candidate>lastRouteIndex+24)candidate=lastRouteIndex+24}candidate=Math.max(0,Math.min(pts.length-1,candidate));if(lastRouteIndex<0||candidate>=lastRouteIndex)lastRouteIndex=candidate;target=pts[lastRouteIndex];const lookAhead=pts[Math.min(pts.length-1,lastRouteIndex+5)];if(lookAhead&&distanceMeters(target,lookAhead)>2)routeHeading=bearing(target,lookAhead);updatePassedLine(pts)}else if(Number.isFinite(Number(lastFix?.heading))){routeHeading=Number(lastFix.heading)}
    displayFix=smoothPoint(displayFix,target,displayFix?.lat?0.3:1);if(!userMarker)userMarker=L.marker([displayFix.lat,displayFix.lon],{icon:markerIcon(),interactive:false,zIndexOffset:1000}).addTo(map);else{userMarker.setLatLng([displayFix.lat,displayFix.lon]);userMarker.setIcon(markerIcon())}
  }
  function onMapPosition(pos){const accuracy=pos.coords.accuracy||999;if(accuracy>85)return;const here={lat:pos.coords.latitude,lon:pos.coords.longitude};const pts=activeWalkPoints();const currentRouteDistance=pts.length?nearest(here,pts).distance:Infinity;let accept=true;if(lastFix){const moved=distanceMeters(lastFix,here),plausible=Math.max(40,accuracy*1.9);if(moved>plausible){const previousRouteDistance=pts.length?nearest(lastFix,pts).distance:Infinity;const muchBetter=currentRouteDistance+20<previousRouteDistance;const stale=Date.now()-lastFixAt>7000;accept=muchBetter||stale}}
    if(!accept)return;lastFix={...here,heading:pos.coords.heading};lastFixAt=Date.now();showNearbyLandmark(here,accuracy);paintMap(here,accuracy).catch(()=>{})}
  function startMapWatch(){if(mapWatch!=null||!navigator.geolocation)return;mapWatch=navigator.geolocation.watchPosition(onMapPosition,()=>{},{enableHighAccuracy:true,maximumAge:800,timeout:15000})}
  function stopMapWatch(){if(mapWatch!=null)navigator.geolocation.clearWatch(mapWatch);mapWatch=null;lastFix=null;lastFixAt=0;displayFix=null;lastRouteIndex=-1;routeHeading=0;lastLandmarkKey='';lastMapRouteKey='';hideLandmarkAndRestoreMap()}
  function getPosition(){return new Promise(resolve=>{if(!navigator.geolocation)return resolve(null);navigator.geolocation.getCurrentPosition(p=>resolve(p),()=>resolve(null),{enableHighAccuracy:true,maximumAge:3000,timeout:8000})})}
  function showStartNotice(text){let n=document.getElementById('routeStartNotice');if(!n){n=document.createElement('div');n.id='routeStartNotice';document.getElementById('home')?.appendChild(n)}n.textContent=text;setTimeout(()=>n.remove(),4500)}
  const originalStart=window.startTrip;
  if(typeof originalStart==='function')window.startTrip=async function(...args){const pos=await getPosition();const pts=pointsForRoute();if(!pos||pts.length<2)return originalStart.apply(this,args);const here={lat:pos.coords.latitude,lon:pos.coords.longitude},near=nearest(here,pts),startDist=distanceMeters(here,pts[0]),destPts=destinationPoints(),dest=destPts.at(-1)||pts.at(-1),destDist=distanceMeters(here,dest),accuracy=pos.coords.accuracy||0,destThreshold=Math.max(DEST_NEAR_METERS,Math.min(110,accuracy+35));if(destDist<=destThreshold){showStartNotice(selectedRoute==='maor-school-home'?'אתה כבר בבית 🏠':'אתה כבר בבית הספר 🏫');navigator.vibrate?.(120);return null}if(startDist<=Math.max(START_NEAR_METERS,accuracy+30))return originalStart.apply(this,args);if(near.distance<=Math.max(ROUTE_NEAR_METERS,accuracy+25)){sessionStorage.setItem('simpleRouteResumeIndex',String(near.index));showStartNotice('נראה שאתה כבר בדרך — מתחילים מהמיקום שלך 📍');return originalStart.apply(this,args)}showStartNotice('אתה לא ליד הדרך המוכרת. אפשר לבקש עזרה מאבא.');navigator.vibrate?.([180,100,180]);return null};
  setInterval(()=>{const tripVisible=!document.getElementById('trip')?.classList.contains('hidden');if(tripVisible&&current?.step?.type==='walk')startMapWatch();else{stopMapWatch();document.getElementById('walkMiniMap')?.classList.add('hidden')}},700);
  addStyles();ensureContainer();
})();
